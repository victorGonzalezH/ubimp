import { SwitchMulticasePipe } from './switch-multicase.pipe';

describe('SwitchMulticasePipe', () => {
  it('create an instance', () => {
    const pipe = new SwitchMulticasePipe();
    expect(pipe).toBeTruthy();
  });
});
